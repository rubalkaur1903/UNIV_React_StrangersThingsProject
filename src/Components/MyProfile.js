import React from 'react';

const MyProfile = ({posts, setPosts}) => {
    return <>
        <h1>My Profile</h1>
        {/* {
            posts.isAuthor && posts.messages ? <div>{}</div> : ''
        } */}
    </>
}

export default MyProfile;